# Add all components here using ADD_COMPONENT(<COMPONENT_DIRECTORY>)

ADD_COMPONENT(StereoEstimator)

#ADD_COMPONENT(DepthConverter)

ADD_COMPONENT(DepthRainbow)

#ADD_COMPONENT(Calibrate)

#ADD_COMPONENT(Undistort)
